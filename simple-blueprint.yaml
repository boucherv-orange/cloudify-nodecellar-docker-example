tosca_definitions_version: cloudify_dsl_1_2

imports:
  - http://www.getcloudify.org/spec/cloudify/3.3.1/types.yaml
  - http://www.getcloudify.org/spec/docker-plugin/1.3.1/plugin.yaml

inputs:

  host_ip:
      description: >
        The ip of the host the application will be deployed on
      default: 127.0.0.1

  agent_user:
      description: >
        User name used when SSH-ing into the started machine
      default: cloud

  agent_private_key_path:
      description: >
        Path to a private key that resided on the management machine.
        SSH-ing into agent machines will be done with this key.
      default: ~/.ssh/agent_key.pem

node_templates:

  host:
    type: cloudify.nodes.Compute
    properties:
      install_agent: true
      ip: { get_input: host_ip }
      cloudify_agent:
        user: { get_input: agent_user }
        key: { get_input: agent_private_key_path }

  clearwater_mgmt_container:
    type: cloudify.docker.Container
    properties:
      name: clearwater-management
      image:
        repository: boucherv/clearwater-management
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: docker.docker_plugin.tasks.create_container
          inputs:
            params:
              detach: true
              stdin_open: true
              tty: true
              command: service clearwater-etcd restart && bash
        start:
          implementation: docker.docker_plugin.tasks.start
          inputs:
            params:
              network_mode: host
              binds: 
                - /home/cloud/local_config:/etc/clearwater/local_config
    relationships:
      - type: cloudify.relationships.contained_in
        target: host



